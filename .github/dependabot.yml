version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows current; Dependabot updates the SHA
  # and the version comment next to it together.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ["*"]
